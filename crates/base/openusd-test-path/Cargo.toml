[package]
name = "openusd-test-path"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Resolve OpenUSD pxr/.../testenv paths from OPENUSD_SRC_ROOT (test-only helper)"
license = "Apache-2.0 OR MIT"
publish = false

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
