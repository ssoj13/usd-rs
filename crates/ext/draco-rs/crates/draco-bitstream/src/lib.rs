//! Draco bitstream encode/decode (mesh and point cloud compression).
//!
//! This crate ports `_ref/draco/src/draco/compression/*` and provides the
//! decode/encode entry points for `.drc` bitstreams.

pub mod compression;
